overwrite: true
schema: "http://localhost:4000/graphql"
documents: "src/graphql/**/*.graphql"
generates:
  src/graphql/API.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
